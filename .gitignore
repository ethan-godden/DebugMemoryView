.DS_Store

# Eclipse workspace state — local, churns constantly, should not be tracked
/.metadata/

# Maven / Tycho build output
target/
# PDE compiled output (per-module)
bin/
# Tycho-generated consumer POMs (regenerated every build)
.tycho-consumer-pom.xml
